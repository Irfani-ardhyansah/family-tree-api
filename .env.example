# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=family_tree

# CORS — comma-separated origins; use * for local open access
CORS_ORIGINS=*

# Auth (used from Part 2+)
JWT_SECRET=change-me-in-production
ACCESS_TTL=3600
REFRESH_TTL_REMEMBER=2592000
REFRESH_TTL_SESSION=86400
